OPENAI_API_KEY=sk-...
# optional: override transcription model
# STT_MODEL=gpt-4o-mini-transcribe

# agent models (segmenter). Extractor = per-concept (strong); judge = per-utterance (fast).
# AGENT_MODEL=gpt-4.1
# AGENT_JUDGE_MODEL=gpt-4.1-mini
# AGENT_EXTRACT_MODEL=  # overrides AGENT_MODEL for the extractor only
